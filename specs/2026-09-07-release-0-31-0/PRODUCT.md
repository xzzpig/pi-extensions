# Release 0.31.0

Publish the completed optimizations from main to npm and GitHub. Release notes must simply quantify improvements against the previous published release, v0.30.5, while preserving the distinction between measured model-context size and actual token usage. Preserve all core features and saved data compatibility.

Use a minor release for the compatible paginated retrieval and atomic task-batch additions. The existing US$5 live-evaluation allowance remains shared; this release requires no new model calls.
