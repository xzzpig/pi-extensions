# Runtime and token optimization

Give runtime responsiveness and total token use equal priority while preserving all shipped capabilities: regular/Sisyphus goals, guided drafting, task trees and evidence, verification contracts, independent audit, optional Oracle, persistence/recovery, budgets, controls, and dashboard.

Use compact, losslessly retrievable context; advertise applicable tools; support atomic ordered task batches and existing single-task calls. Preserve saved goals, session history, ordinary work tools, and human-owned focus/confirmation. No extra summarizer calls or periodic UI polling.

Acceptance: corrected request accounting; at least 25% lower extension-attributable active-workflow context; at least 2x faster long-history activity rendering; no material regressions; full tests/check/lint and SDK compatibility. Live paired smoke evaluation is authorized with a combined US$5 ceiling, including child audits/Oracle; report incomplete coverage and do not infer statistical equivalence.

## Live model preference

The user selected DeepSeek V4 Flash on OpenCode for paired live evaluation. Use identical reasoning settings for both versions. Earlier fallback-model requests and conservatively reserved interrupted requests remain included in the original US$5 combined allowance; report them separately.
