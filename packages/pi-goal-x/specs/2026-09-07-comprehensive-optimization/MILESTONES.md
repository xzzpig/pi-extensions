# Implementation log

- Merged the previous three validated commits into local `main` using a fast-forward. No remote push was requested or performed.
- Started a fresh comprehensive campaign against `5e16557`. Initial audit identified repeated settings resolution, content-key allocation, repeated history serialization during paging, redundant activity formatting, and session-health parsing as additional candidates. Existing auditor prompts already deduplicate objective/task data; remaining repeated policy wording is under review.
- Implemented bounded settings/task/prompt/detail/text caches, copied mutable public results, optimized task mutation paths and open-goal counts, reduced checkpoint-message parsing and auditor stream-preview work, and retained complete reports and requirements.
- Corrected two derived-storage defects found during the audit: timestamped archive paths were not removed from pool snapshots, and reconstructed Oracle results were omitted from ledger checkpoints. Checkpoint version 3 rebuilds older derived data; corrupt Oracle/accumulator state falls back to the ledger.
- The first focused test invocation used the benchmark adapter, exposing its missing fsync export. Added counted descriptor/fsync operations, copied the corrected adapter into the isolated baseline, and remeasured both sides. Full validation uses the actual test runner and supported SDKs.
- All 922 tests and TypeScript passed after implementation. A first 100,000-event cold-history timing was 29% slower despite lower allocation; repeating in alternating baseline/candidate order before deciding whether this is a regression.
- DeepSeek V4 Flash (`opencode-go`, high reasoning) passed both incorrect-artifact rejection and corrected-artifact approval on baseline and candidate. Eleven requests cost US$0.002982788; cumulative original-campaign spend/uncertain-call charges total US$1.032791791, with no outstanding reservations. Small-sample evidence only.
- The stricter gate exposed a 10,000-line diagnostic slowdown and uneven cold-start samples. CPU profiles showed unchanged JSON parsing work but repeated goal-ID sanitation; added a bounded per-read intern map. Replaced the manual JavaScript diagnostic line scanner with native splitting while continuing to discard parsed entries immediately. Retained initial timing files and repeated the complete campaign after these fixes.

- Final acceptance: 923/923 full-suite tests; TypeScript; lint; 888/888 serial tests on SDK 0.84.1 and again on 0.83.0; 24-fixture context gate; six actual outgoing provider-payload checks on each SDK; fresh 94-case benchmark gate and stricter comprehensive gate all pass. The older SDK checkout lacked the newer npm provider-check alias; invoking the same checked-in script directly passed.
- Final alternating 100,000-event cold rebuild p50 averages: 86.74 ms baseline, 80.24 ms candidate. The previously failing 10,000-event case is now 10.27 → 10.61 ms, within the predeclared non-regression allowance. Initial unsuccessful measurements remain available.

- Landed measurement setup first (`dba8195`), then runtime changes (`4df847a`). The isolated runtime-only tree passed TypeScript, 922 tests and the unchanged 24-fixture context gate before its commit. Model-facing auditor/compaction changes and final evidence follow as the third commit on local `main`.
