{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "title": "@xzzpig/pi-terminal-notifications configuration",
  "type": "object",
  "additionalProperties": false,
  "properties": {
    "fallback": {
      "description": "Protocol used when TERM_PROGRAM is unset or has no matching mapping.",
      "enum": ["osc9", "osc99", "osc777"]
    },
    "termPrograms": {
      "description": "TERM_PROGRAM to terminal notification protocol mappings. These entries override or extend the built-in defaults.",
      "type": "object",
      "additionalProperties": {
        "enum": ["osc9", "osc99", "osc777"]
      }
    }
  }
}
